
%hr

%p= button_to("Start a new game", new_game_url, :remote => true, :class => t(:start_game)[:new_game], :id => t(:start_game)[:new_game])
//%p= button_to "Join a game", join_game_url, :remote => true, :class => 'join_game', :id => 'join_game'
