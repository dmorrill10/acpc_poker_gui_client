#rejoin_match_form
   = form_tag rejoin_match_url, remote: true do
      = label_tag 'Match name'
      = text_field_tag 'match_name'
      = submit_tag 'Rejoin match', class: 'button'
   .hidden= hidden_game_parameter_form